import 'package:flutter/material.dart';

class DeptoFormPage extends StatelessWidget {
  const DeptoFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Depto Form')));
  }
}
