import 'package:flutter/material.dart';

class DeptosPage extends StatelessWidget {
  const DeptosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Deptos Page')));
  }
}
