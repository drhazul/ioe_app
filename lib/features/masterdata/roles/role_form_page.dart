import 'package:flutter/material.dart';

class RoleFormPage extends StatelessWidget {
  const RoleFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Role Form')));
  }
}
